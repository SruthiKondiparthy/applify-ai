{
  "version": 2,
  "buildCommand": "npm run build --prefix frontend",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs",  
  "installCommand": "npm install --prefix frontend",  
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://applify-backend.vercel.app/:path*"
    }
  ]  
}
