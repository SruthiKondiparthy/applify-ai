name: Backend CI/CD

on:
  push:
    branches: [ "main", "master" ]
    paths-ignore:
      - 'frontend/**'
  pull_request:
    branches: [ "main", "master" ]
    paths-ignore:
      - 'frontend/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.11"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        if [ -d "tests" ]; then
          pytest
        else
          echo "No tests folder found — skipping"
        fi

    - name: Lint code (flake8)
      run: |
        pip install flake8
        flake8 . || true

    - name: Build Docker image
      run: |
        docker build -t applify-backend -f Dockerfile.backend .
        docker build -t applify-ui -f Dockerfile.ui .

    - name: Deploy to Vercel
      if: (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master') && github.event_name == 'push' && secrets.VERCEL_TOKEN != ''
      uses: amondnet/vercel-action@v25
      with:
        vercel-token: ${{ secrets.VERCEL_TOKEN }}
        vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
        vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
        vercel-args: '--prod'

